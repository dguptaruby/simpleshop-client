const authReducer = (state = 0, action) => {
    switch (action.type) {
        case "login":
                return state
            break;
    
        default:
            return state
    }
}

export default authReducer